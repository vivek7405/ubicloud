#!/bin/bash
# Resume a paused VM. Called by pilot-platform via SSH.
# Usage: pilot-restore <inhost_name>
set -e

VM="$1"
CHR="/opt/cloud-hypervisor/v46.0/ch-remote"
SOCK="/vm/$VM/ch-api.sock"

$CHR --api-socket "$SOCK" resume
